const initialState = {
  isLoading: false,
}

const common = (state = initialState, action) => {
  const { type } = action

  switch (type) {
    default:
      return state
  }
}

export default common
